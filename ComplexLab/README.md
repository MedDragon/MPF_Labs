📚 ComplexLab: Spring Boot & PostgreSQL Application
Розробник: Максим Шелудченко

Проєкт являє собою багаторівневу (multi-module) систему на базі Maven, спроектовану для розгортання в Docker-контейнерах та хмарному сервісі Render.

🏗 Архітектура системи
Проєкт розділений на три логічні модулі для забезпечення чіткого розподілу відповідальності:

core: Бізнес-сутності (domain models) та інтерфейси сервісів.

persistence: Робота з базою даних через Spring Data JPA, реалізація сервісів та автоматичне керування схемою через Flyway.

web: Клієнтська частина (Thymeleaf), механізми безпеки (Spring Security), REST-контролери, моніторинг (Actuator) та конфігурація Docker.

🛠 Локальний запуск та налагодження
1. Підготовка бази даних
Для коректної роботи додаток очікує наявність PostgreSQL зі наступними параметрами:

DB Name: complexlab

User: complexlab_user

Password: complexlab_pass

2. Збірка та старт
Виконайте збірку всього проєкту та запуск модуля web:

Bash

mvn clean package -pl web -am
java -jar web/target/web-*.jar
Після старту додаток доступний за адресою: http://localhost:8080.

3. Авторизація за замовчуванням
При першому запуску система автоматично створює адміністратора (якщо він відсутній):

Login: admin

Password: admin123 (можна змінити через змінні оточення APP_ADMIN_PASSWORD).

🐳 Робота з Docker
Додаток повністю контейнеризований. Для локального запуску образу використовуйте наступні команди:

Bash

# Збірка образу
docker build -t complexlab-web -f web/Dockerfile .

# Запуск з прокиданням порту та налаштуванням БД
docker run -p 8080:8080 \
  -e SPRING_DATASOURCE_URL=jdbc:postgresql://host.docker.internal:5432/complexlab \
  -e SPRING_DATASOURCE_USERNAME=complexlab_user \
  -e SPRING_DATASOURCE_PASSWORD=complexlab_pass \
  complexlab-web
☁️ Деплой на платформу Render
Процес розгортання розділений на два етапи:

Крок 1: База даних
Створіть екземпляр Render PostgreSQL. Скопіюйте Internal Database URL для подальшого використання.

Крок 2: Веб-сервіс
Підключіть свій репозиторій GitHub.

Оберіть середовище Docker та вкажіть шлях до файлу: web/Dockerfile.

Налаштуйте Environment Variables:

SPRING_DATASOURCE_URL (Ваш Internal URL з кроку 1)

SPRING_DATASOURCE_USERNAME

SPRING_DATASOURCE_PASSWORD

🔍 Моніторинг та перевірка
Для контролю стану системи використовуйте вбудовані ендпоінти Spring Actuator:

Статус системи: http://localhost:8080/actuator/health
